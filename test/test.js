'use strict';

var expect = require('chai').expect;
var event = require('../index');

describe('#group of test name', function() {
    it('test name', function() {
        expect(true).to.exist;
    });

});
